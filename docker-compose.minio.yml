services:
  minio:
    # Pinned community S3 emulator: quay.io/minio/minio no longer permits pulls.
    image: localstack/localstack:4.14.0
    environment:
      SERVICES: s3
    ports:
      - "9002:4566"
